name: dependency-review

on:
  pull_request:

jobs:
  dependency-review:
    runs-on: ubuntu-latest
    steps:
      - id: build_docs
        uses: it-at-m/lhm_actions/action-templates/actions/action-dependency-review@0adbb607339cb32296289c8c319970b9ef2121cb # v1.0.2
# (Line 12 is intentionally left blank with no trailing spaces)
