name: Trivy security scan
on:
  pull_request:
jobs:
  scan:
    name: Security Check
    runs-on: ubuntu-latest
    steps:
      - name: Run Trivy Security check on Repository
        uses: it-at-m/lhm_actions/action-templates/actions/action-trivy@trivy-anpassungen
