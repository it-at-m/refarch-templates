id: add and configure local client
author: Default
realm: local_realm
changes:
  - addSimpleClient:
      clientId: local
      secret: client_secret
      publicClient: false
      redirectUris:
        - http://*
        - https://*
